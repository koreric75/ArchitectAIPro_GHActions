%% Generated by Architect AI Pro | BlueFalconInk LLC
%% https://architect-ai-pro-mobile-edition-484078543321.us-west1.run.app/
graph TD
    subgraph "BlueFalconInk LLC - ArchitectAIPro_GHActions Architecture"
        subgraph "External Systems"
            GitHubUser[GitHub User]
            GitHubActions[GitHub Actions]
            GoogleGemini[Google Gemini API]
            DrawioCLI[Draw.io CLI]
            MermaidInk[mermaid.ink API]
        end

        subgraph "Security"
            CloudArmor[Cloud Armor · WAF]
            CloudLB[Cloud Load Balancer]
            WIF[Workload Identity Federation]
            SecretManager[Cloud Secret Manager]
        end

        subgraph "Application"
            ArchitectAIProGHActions[Architect AI Pro Workflows]
            DiagramGenerator[Diagram Generator Script]
            ForemanAudit[Foreman Audit Script]
            RepoAuditor[Repo Auditor Script]
            CleanupAgent[Cleanup Agent Script]
            DashboardGenerator[Dashboard Generator Script]
            ProdReadiness[Prod Readiness Script]
            GalleryApp[Architecture Gallery App · Cloud Run]
            Terraform[Terraform IaC]
        end

        subgraph "Data"
            CloudStorage[Cloud Storage · Artifacts]
            AuditReport[Audit Report JSON]
            DiagramFiles[Diagram Files · Mermaid/Draw.io/PNG]
        end

        GitHubUser -->|Triggers workflows| GitHubActions
        GitHubActions -->|Orchestrates execution| ArchitectAIProGHActions
        ArchitectAIProGHActions -->|Executes| DiagramGenerator
        ArchitectAIProGHActions -->|Executes| ForemanAudit
        ArchitectAIProGHActions -->|Executes| RepoAuditor
        ArchitectAIProGHActions -->|Executes| CleanupAgent
        ArchitectAIProGHActions -->|Executes| DashboardGenerator
        ArchitectAIProGHActions -->|Executes| ProdReadiness
        ArchitectAIProGHActions -->|Applies infrastructure| Terraform
        ArchitectAIProGHActions -->|Authenticates via| WIF
        WIF -->|Accesses secrets| SecretManager
        WIF -->|Manages resources| CloudStorage
        WIF -->|Deploys services| GalleryApp

        DiagramGenerator -->|Sends code context| GoogleGemini
        GoogleGemini -->|Returns Mermaid diagram| DiagramGenerator
        DiagramGenerator -->|Generates| DiagramFiles
        DiagramGenerator -->|Renders PNG| DrawioCLI
        DiagramGenerator -->|Renders PNG fallback| MermaidInk
        DrawioCLI -->|Outputs PNG| DiagramFiles
        MermaidInk -->|Outputs PNG| DiagramFiles
        DiagramFiles -->|Stored in| CloudStorage

        ForemanAudit -->|Reads| DiagramFiles
        ForemanAudit -->|Generates| AuditReport
        RepoAuditor -->|Queries GitHub API| GitHubActions
        RepoAuditor -->|Generates| AuditReport
        AuditReport -->|Stored in| CloudStorage

        CleanupAgent -->|Modifies repos via| GitHubActions

        DashboardGenerator -->|Reads| AuditReport
        DashboardGenerator -->|Generates HTML| CloudStorage

        Terraform -->|Deploys/Manages| GalleryApp
        Terraform -->|Configures| CloudArmor
        Terraform -->|Configures| CloudLB
        Terraform -->|Manages| SecretManager
        Terraform -->|Manages| CloudStorage

        GitHubUser -->|Accesses Gallery via HTTPS| CloudLB
        CloudLB -->|Protects| CloudArmor
        CloudArmor -->|Routes traffic to| GalleryApp
        GalleryApp -->|Serves diagrams from| CloudStorage
        GalleryApp -->|Retrieves runtime secrets from| SecretManager

        FOOTER[🏗️ Created with Architect AI Pro · BlueFalconInk LLC]
    end

    style Security fill:#1E40AF,color:#BFDBFE
    style Application fill:#1E3A5F,color:#BFDBFE
    style Data fill:#0F172A,color:#BFDBFE
    style FOOTER fill:#1E40AF,color:#BFDBFE,stroke:#3B82F6